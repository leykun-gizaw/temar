services:
  temar_db:
    image: postgres:16-alpine
    container_name: prod_db
    restart: always
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}
    volumes:
      - pg_data_prod:/var/lib/postgresql/data
    networks:
      - temar_net

  web:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/temar-web:latest
    container_name: prod_web
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - temar_db
      - migrate
    networks:
      - temar_net

volumes:
  pg_data_prod:

networks:
  temar_net:
